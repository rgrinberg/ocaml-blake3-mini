(lang dune 3.18)

(context
 (default
  (name blake3-amd64-asm)
  (profile blake3-amd64-asm)))

(context
 (default
  (name blake3-x86-intrinsics)
  (profile blake3-x86-intrinsics)))

(context
 (default
  (name blake3-neon-intrinsics)
  (profile blake3-neon-intrinsics)))

(context
 (default
  (name blake3-none)
  (profile blake3-none)))
